{
  "version": 2,
  "framework": "vite",
  "installCommand": "corepack enable && corepack use pnpm@10.4.1 && NPM_CONFIG_FROZEN_LOCKFILE=false pnpm install --no-frozen-lockfile",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
