{
  "version": 2,
  "framework": "vite",
  "installCommand": "corepack enable && corepack use pnpm@10.4.1 && pnpm install",
  "buildCommand": "pnpm run build:vercel",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
